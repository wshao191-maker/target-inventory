# 目标库存测算：UI 说明

## 整体布局

- 单页应用，默认浅色主题，界面文案为中文。
- 自上而下：应用标题与说明 → 左右两栏（左表单、右结果）→ 页脚提示。
- 宽屏为左右两栏，窄屏（≤900px）自动堆叠为上下排列，不出现横向滚动。

## 左栏：产品参数

- 表单为自适应网格，成对排列输入框；产品名称与波动性口径占满整行。
- 每个字段包含：标签、单位角标、输入框、灰色说明文字；出错时该字段下方出现红色错误提示。
- 「预测准确率 / MAPE」为分段按钮切换，切换后标签与换算提示同步变化（如准确率 85% 对应 MAPE 15%）。
- 选填项标签带「选填」角标，占位符提示默认值 0。
- 底部为通栏主按钮「计算目标库存」。
- 提交时若校验不通过，表单顶部出现红色汇总条：「有 N 项需要修正，请检查标红的字段。」

## 右栏：计算结果

初始状态显示虚线框提示「填好左侧参数，点击『计算目标库存』查看结果」。

计算后自上而下展示：

1. 呈现口径切换：右上角分段按钮「按件数 / 按天数」，默认按件数；切换后主结果与四段构成同步换算。
2. 目标库存水平：大号蓝色数字 + 单位（件 或 天），下方一行小字给出原始小数、取整说明与另一种单位的折算值。
3. 四段构成：安全库存、周转库存、提前期需求、冻结库存四行，每行为名称 + 占比条 + 当前单位数值，小字显示占比与另一种单位。四条用不同颜色区分。
4. 关键中间量：日均需求、MAPE、σ需求（主口径）、Z 值，四张小卡片。
5. 换算过程：编号步骤列表，每一步写出算式与结果；月需求大于 0 时末尾附库存天数换算。
6. 参考口径提示条（浅蓝底）：「若把 σ需求 换成 1.25 × MAPE × 日均需求……」。
7. 建议下单量提示条：正常为浅绿底；当缺口不足一个 MOQ 时切换为浅黄底并说明受 MOQ 约束。内容含天数换算。
8. 「计算公式说明」折叠区，点开可见全部公式与参考口径说明。
9. 「复制结果」按钮，复制成功后右侧短暂出现「已复制到剪贴板」。

## 页脚

一句话说明参数保存在本机浏览器、工具不上传任何数据。

## 视觉约定

- 主色：蓝色（#2563eb）；错误红色；提示绿/黄。
- 卡片圆角 14px，浅灰描边与极浅阴影。
- 数字使用等宽数字字形（tabular-nums）对齐，件数保留千分位。
