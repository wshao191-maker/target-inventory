# 目标库存测算：UI 说明

## 整体布局

- 单页应用，默认浅色主题，界面文案为中文，零依赖。
- 自上而下：应用标题与说明 → 左右两栏（左表单、右结果）→ 页脚提示。
- 宽屏为左右两栏，窄屏（≤900px）自动堆叠为上下排列，不出现横向滚动。

## 左栏：产品参数

表单按业务含义分为三组，每组带竖条标记的小标题：

- **需求与波动**：产品名称/SKU、月需求、月计天数、预测准确率/MAPE
- **供应与订货**：采购提前期、提前期波动 σ、下单周期、MOQ
- **服务与库存**：服务水平、冻结天数、现有库存

字段采用响应式栅格，宽屏两列、窄屏单列；产品名称与准确率输入占满整行。

每个字段包含：标签、单位角标、右侧圆形问号按钮、输入框；校验失败时字段下方出现红色提示。

字段说明收进问号气泡：桌面悬停或聚焦即显示，点击可固定；移动端点击展开，点击气泡外部或按 Esc 关闭。气泡带 `role="tooltip"` 与 `aria-expanded`，键盘 Tab 可达。

「预测准确率 / MAPE」为分段按钮切换，切换后标签与气泡内文案同步变化（如准确率 85% 对应 MAPE 15%）。

底部为通栏主按钮「计算目标库存」；校验失败时表单顶部出现红色汇总条「有 N 项需要修正」。

## 右栏：计算结果

初始为空状态：虚线框 + ∑ 图标 + 引导文案。

计算后自上而下展示：

1. **呈现口径切换**：「按件数 / 按天数」分段按钮，默认按件数。
2. **主结果卡片**：浅靛蓝渐变底，大号靛蓝数字与单位，下方小字给出原始值与另一种口径的折算说明。
3. **堆叠构成条**：一条 100% 宽圆角横条，四段按占比着色、段间留白，计算完成后自左向右展开；数值为 0 的段不渲染，避免出现空缝。
4. **四段构成**：安全库存、周转库存、提前期需求、冻结库存；每行为「同色圆点 + 名称 + 当前单位数值 + 占比与另一单位的小字」。
5. **建议下单量**：浅绿底提示条；缺口不足一个 MOQ 时切换为浅黄底并说明 MOQ 约束。
6. **关键中间量**：日均需求、MAPE、σ需求（主口径）、Z 值，四张小卡片。
7. **「换算过程与参考口径」折叠区**：默认收起，展开后依次为换算过程步骤、参考口径、计算公式说明。
8. **「复制结果」按钮**：复制成功后右侧短暂出现「已复制到剪贴板」。

## 视觉约定

- 主色靛蓝 `#4f46e5`，悬停态 `#4338ca`，浅色底 `#eef2ff`。
- 页面底为极淡纵向渐变；卡片 18px 圆角配两层柔和阴影，输入框 10px 圆角、按钮 12px 圆角。
- 四段构成配色：蓝 `#2563eb`、青 `#0891b2`、绿 `#059669`、琥珀 `#d97706`，图例小字使用各自的深色版本。
- 全部文字与其背景的对比度均 ≥4.5:1，满足 WCAG AA；数字统一使用等宽数字字形对齐。

## 动效

- 结果首次出现时淡入上移，四段明细、建议下单量、中间量依次轻微延迟；切换呈现口径时不重播动画。
- 堆叠构成条首次计算时自 0 展开。
- 按钮悬停轻微抬起、按下回弹；分段按钮带过渡。
- 系统开启「减少动态效果」（`prefers-reduced-motion`）时全部关闭。

## 页脚

一句话说明参数保存在本机浏览器、工具不上传任何数据。
