# 目标库存测算：功能需求

## 一句话说明

填一张表单，算出一个产品的目标库存水平，并看清每一段数字的来源。

## 计算范围

目标库存按「峰值目标库存」口径，由四段相加构成：

1. 安全库存：应对需求波动与提前期波动
2. 周转库存：覆盖一个下单周期需求与 MOQ 中较大者的半个批量（平均周转占用）
3. 提前期需求：覆盖采购提前期内的正常消耗
4. 冻结库存：覆盖被质检或占用、暂时无法动用的部分

一次只计算一个 SKU。

## 操作流程

1. 填写产品参数（只有产品名称与三项选填项可以留空）。
2. 点击「计算目标库存」。
3. 右侧结果区展示目标库存、四段构成、关键中间量、换算过程、参考口径与建议下单量。
4. 如有需要，点击「复制结果」把纯文本摘要发给同事。

## 计算细节

- 日均需求 = 月需求 ÷ 月计天数。
- MAPE = 1 − 预测准确率；也可直接切换到 MAPE 口径录入。
- σ需求 = MAPE × 日均需求；参考口径为 1.25 × MAPE × 日均需求。
- Z 值由服务水平经标准正态分布反函数换算，显示 3 位小数。
- 安全库存 = Z × √(提前期 × σ需求² + 日均需求² × 提前期波动²)。
- 周转库存 = 0.5 × max(日均需求 × 下单周期, MOQ)。
- 冻结库存 = 日均需求 × 冻结天数。
- 目标库存 = 上述四段之和，向上取整为整数件。
- 建议下单量：现有库存 ≥ 目标库存时为 0；否则 max(MOQ, ceil(目标库存 − 现有库存))。
- 库存天数 = 数量 ÷ 日均需求，作为结果的另一种呈现口径；月需求为 0 时显示「—」。

## 结果呈现

- 结果区顶部提供「按件数 / 按天数」切换，默认按件数，选择会保存在本机。
- 切换只改变展示单位，不改变任何计算结果；两种单位会互相作为副信息展示。
- 复制结果时同样沿用当前选择的呈现口径，并附带另一种单位。

## 校验

- 月需求、月计天数、采购提前期、下单周期、MOQ、预测准确率/MAPE 为必填。
- 提前期波动、冻结天数、现有库存留空视为 0。
- 非数字、负数、月计天数 ≤ 0、服务水平越界均阻止计算并提示。

## 数据留存

- 参数实时写入浏览器 localStorage，刷新或关闭后重新打开仍然存在。
- 不提供历史记录、导入导出、账号与多端同步。

## 不在本版范围内

- 多 SKU 批量计算与 CSV/Excel 导入导出
- 补货点、在途库存、多级库存与 MRP 联动
- 服务水平 ↔ Z 值的手工查表模式
